using UnityEngine;

public class CameraSetting : MonoBehaviour
{
    /*
    public CameraLimit limit;

    private void Start()
    {
        GameManager.Instance.cam.GetComponent<CameraController>().SetLimit(limit);
    }*/
}
